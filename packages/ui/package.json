{
  "name": "@shootgit/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Solid + Vite frontend. Talks to the Rust core only through Tauri commands and events (SPEC §4).",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@shootgit/ipc-types": "workspace:*",
    "solid-js": "^1.9.15"
  },
  "devDependencies": {
    "@tauri-apps/api": "^2.11.1",
    "jsdom": "^30.1.1",
    "typescript": "^7.0.2",
    "vite": "^8.3.0",
    "vite-plugin-solid": "^2.11.14",
    "vitest": "^5.0.1"
  }
}
