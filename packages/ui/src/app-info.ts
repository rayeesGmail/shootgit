/**
 * `APP_NAME` is a placeholder: the product name is still an open question
 * (SPEC §12). The same placeholder is spelled out in `index.html`
 * (`<title>`), `src-tauri/tauri.conf.json` (`productName`, window title) and
 * the bundle identifier `dev.placeholder.shootgit`. Renaming means touching
 * all four plus `src/mount.test.tsx`.
 *
 * Not localised: `t()` scaffolding arrives with P6-20, and a product name is
 * not translated anyway.
 */
export const APP_NAME = 'Shootgit';
