# docs/

- `SPEC.md` — export of the living spec (Claude Doc). **Not included in this bundle:** open the spec, use Export → Markdown, save here as `SPEC.md`. Re-export whenever the spec changes; never hand-edit.
- `plans/` — phase task checklists Claude Code works from (`/task`).
- `ADR/` — architecture decision records.
