# Git client (working title)

IntelliJ-grade Git tooling as a small, fast, standalone app for macOS, Windows and Linux, with GitHub and GitLab pull requests built in. Free in 1.0.

Start here:
1. `CLAUDE.md` — rules Claude Code follows in this repo.
2. `docs/SPEC.md` — the spec (export it from the Claude Doc first, see `docs/README.md`).
3. `docs/plans/README.md` — the phase plan. Run `/task` in Claude Code to begin Phase 0.
